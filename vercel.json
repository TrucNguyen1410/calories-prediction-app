{
  "installCommand": "if cd flutter; then git pull && cd .. ; else git clone --depth 1 -b stable https://github.com/flutter/flutter.git; fi && flutter/bin/flutter config --enable-web",
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}